
  # Personal Portfolio Development

  This is a code bundle for Personal Portfolio Development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
